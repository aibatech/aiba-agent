# User Profile

This is a safe example. AIBA creates each real user's private profile inside the local
`agent_system/profiles/` directory, which must never be committed.

- Preferred name:
- Role or occupation:
- Business or organization:
- Main goal:
- Current projects:
- Communication style: Friendly
- Detail level: Simple
- Initiative: Balanced
- Humor: Light
- Memory: Active
- Actions that always require approval:
- Things AIBA should not automate:

Users can review their profile with `/profile`, pause learning with `/memory pause`, and
resume it with `/memory resume`.
