__pycache__/
*.pyc
.env

# Local runtime / data directory (DBs, backups, vault, logs, workspace, user profiles) — never commit
agent_system/
# Private per-user personality profiles (name, work, goal, style) — never commit
agent_system/profiles/

# Virtualenv + build artifacts
.venv/
build/
certification/
*.egg-info/
